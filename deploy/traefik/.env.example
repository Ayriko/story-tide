# Placeholders uniquement — jamais de vraie valeur committee. Copier en
# `deploy/traefik/.env` sur le VPS (a la main, jamais dans le repo).

# Email de contact Let's Encrypt (notifications d'expiration de certificat).
ACME_EMAIL=admin@storytide.fr
