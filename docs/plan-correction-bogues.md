# Plan de correction des bogues — C2.3.2

> Alimenté par les anomalies/régressions détectées en recette.

## SLA / priorisation

- **P0** (bloquant : perte de données, faille de sécurité, prod down) : corrigé sous 24 h.
- **P1** (fonctionnalité majeure dégradée) : corrigé sous 72 h.
- **P2** (mineur, cosmétique) : planifié au jalon suivant.

Processus : détection → qualification (description, impact, reproduction) →
priorisation → diagnostic → correctif **+ test unitaire de non-régression** →
recette → déploiement. Alimenté par les issues GitHub (label `bug`) — pas encore
de repo GitHub actif à ce stade (remote configuré, pas d'issues créées).

| ID bug | Origine (TST-###) | Sévérité | Analyse cause | Correctif | Statut |
|---|---|---|---|---|---|
| BUG-001 | TST-AUT-007 | P1 | Après une erreur de soumission sur `/login` ou `/register` (mauvais mot de passe, e-mail déjà pris, champ manquant), **tous les champs du formulaire se vidaient**, y compris ceux correctement remplis. Cause : React 19 réinitialise les champs non contrôlés d'un `<form action={formAction}>` dès que la fonction d'action **se résout**, même si elle renvoie un état d'erreur — pas seulement en cas de succès (confirmé via issues GitHub React #29034 et #31649). | `src/actions/auth.ts` renvoie désormais les valeurs soumises (`values: { name?, email? }`, **jamais** le mot de passe) à chaque retour d'erreur ; `login-form.tsx`/`register-form.tsx` réappliquent ces valeurs via `defaultValue` sur les champs concernés. Décision produit associée : le mot de passe n'est jamais ré-affiché après une erreur (convention GitHub/GitLab), validée avec Aymeric. | ✅ corrigé + test de non-régression `login-form.test.tsx` (« réaffiche l'e-mail saisi après une erreur, mais jamais le mot de passe ») + validé manuellement par Aymeric |
| BUG-002 | TST-ENT-011, TST-SEC-014 | P1 | Remonté en test manuel le 2026-07-21 : coller un texte externe (Obsidian) dans l'éditeur d'entité faisait échouer **toute la sauvegarde** avec « Contenu invalide. », sans rapport apparent avec la cause réelle. Deux symptômes de la même famille (contenu HTML externe collé, jamais rencontré à la frappe manuelle) : (1) un wikilien converti en `<a href="…">` avec un `href` relatif (parfois avec espaces) échouait `isSafeHttpUrl` (URL `http(s)` absolue exigée) côté validation serveur, qui rejette alors le document entier plutôt que le seul attribut fautif ; (2) un retour à la ligne rendu en `<br>` (au lieu d'un paragraphe distinct) fondait deux lignes du texte source en un seul paragraphe ProseMirror. | (1) Extension `SafeLink` (`src/lib/tiptap-extensions.ts`) : retire la mark `link` dès le parsing (frappe/collage) si `isSafeHttpUrl` (déplacée dans `src/lib/url-safety.ts`, source unique de vérité avec la validation serveur) échoue — le texte de l'ancre est conservé, la validation serveur reste inchangée et non contournable. (2) `splitParagraphsOnBreaks` (`src/lib/tiptap-paste.ts`), câblée via `transformPastedHTML` : scinde un `<p>` contenant des `<br>` en paragraphes distincts au collage (DOMParser, pas de regex), sans paragraphe vide pour des `<br>` consécutifs ou en fin de bloc. | ✅ corrigé + tests de non-régression `tiptap-extensions.test.ts` (6 cas `SafeLink` + cas `splitParagraphsOnBreaks` + intégration collage Obsidian complet), gates complets (lint/tsc/322 tests/build/9 e2e) — vérification manuelle Aymeric en attente |
| BUG-003 | TST-GRF-001 | P2 | Remonté en test manuel le 2026-07-21 (KAN-36 P5) : la disposition des nœuds de la Constellation changeait d'un rechargement à l'autre de la **même** page `/graph`. Cause : `listWorldRelations` (`relation-service.ts`) n'avait aucun `orderBy` — Postgres ne garantit aucun ordre de lignes sans lui — et le layout `cose` de Cytoscape (côté client) traite les arêtes dans l'ordre reçu ; un ordre instable fait converger la simulation physique vers une disposition finale différente à chaque appel, même avec `randomize:false`. (Reste, hors correctif : le panneau du dashboard et la page `/graph` auront toujours des positions différentes l'un de l'autre, tailles de canvas différentes → simulation différente — chantier séparé, persistance d'une disposition canonique, noté au backlog KAN-33.) | `orderBy: [{ createdAt: "asc" }, { id: "asc" }]` ajouté à `listWorldRelations` — `id` (cuid, unique) en second critère lève l'ambiguïté d'un `createdAt` à égalité (lot de relations AUTO créées par le worker dans la même milliseconde). | ✅ corrigé + test de non-régression `relation-service.test.ts` (assertion de l'appel exact `findMany` avec `orderBy`), gates complets (lint/tsc/338 tests/coverage) — vérification manuelle Aymeric en attente |
| BUG-004 | TST-MND-008 | P2 | Remonté en test manuel sur staging le 2026-07-21 : créer une entrée via le bouton « Nouvelle entrée » **du dashboard** ne rafraîchissait jamais la Sidebar une fois revenu dessus (la nouvelle entrée manquait tant qu'aucun rechargement complet n'était fait), alors que la même création depuis le bouton de la **sidebar** semblait fonctionner. **Historique complet des tentatives (aucune réécrite après coup)** : (v1) `revalidatePath("/worlds/[slug]", "layout")` ajouté aux trois actions d'entité — retesté par Aymeric le 2026-07-22, **toujours cassé**. (v2) même correctif corrigé pour inclure le groupe de routes (`revalidatePath("/(app)/worlds/[slug]", "layout")`, la Sidebar étant portée par `(app)/worlds/[slug]/layout.tsx`) — retesté par Aymeric, **toujours cassé**. Les deux tentatives ciblaient le mauvais étage (serveur) ; sur demande explicite d'Aymeric (« STOP aux hypothèses »), diagnostic par preuve : logs temporaires dans `layout.tsx` (compteur serveur) et `entity-search.tsx` (prop reçue vs état affiché à chaque rendu), rejoués via un script Playwright jetable contre le dev server local. **Preuve obtenue** : 10 rendus consécutifs avec `initialEntities.length=1` (prop fraîche, donc le serveur envoyait bien la bonne donnée — les deux tentatives `revalidatePath` n'étaient pas fausses en soi, juste inutiles ici) mais `results.length=0` figé. Cause réelle, `entity-search.tsx:36` : `useState(initialEntities)` ne réutilise l'argument qu'au tout premier montage ; ce composant vit dans un layout persistant à travers toute navigation interne au monde, donc les props plus récentes étaient silencieusement ignorées — un bug **client**, pas serveur. | `entity-search.tsx` : suppression de la copie d'état pour la liste par défaut — sans recherche active, la liste affichée dérive directement de la prop `initialEntities` à **chaque rendu** (`const results = isSearching ? (searchResults ?? []) : initialEntities`). Une recherche active (KAN-17, `searchEntitiesAction`) reste en state (`searchResults`, `null` = pas de recherche) car ses résultats viennent du serveur et ne peuvent pas être dérivés des props. Les deux tentatives `revalidatePath` (v1/v2, inoffensives mais pas la cause) restent en place côté serveur. | ✅ corrigé (v3, cause client) + nouvel e2e de non-régression `e2e/dashboard-create-entity.spec.ts` (création dashboard **et** sidebar, état plié des groupes conservé, recherche active non régressée), gates complets (lint/tsc/341 tests/10 e2e/build) — vérification manuelle Aymeric en attente |
| BUG-005 | TST-LNK-008 | P1 | Diagnostiqué proactivement le 2026-07-22 (pas remonté en recette) : hypothèse initiale d'Aymeric à vérifier avant tout correctif, protocole de preuve empirique par test Vitest imposé (« STOP aux hypothèses »). Un texte en forme Unicode **décomposée** (NFD — accent = caractère combinant séparé, ex. copier-coller depuis macOS/export tiers) matche correctement en isolation, mais `AhoCorasick.search()` (`aho-corasick.ts`) calcule ses positions sur la chaîne normalisée par `normalizeForMatch` (`normalize.ts`, transformation sur la chaîne ENTIÈRE, pas caractère par caractère) puis les réutilise telles quelles contre le texte **original** pour la vérification de frontière de mot — un texte NFD plus tôt dans le document décale cette vérification et fait **disparaître silencieusement** une mention ultérieure, **même non accentuée**, donc jamais de `Relation origin=AUTO` ni de surlignage pour elle. Preuve empirique par test Vitest (`aho-corasick.test.ts`) avant tout correctif, conformément au protocole demandé. | Normalisation NFC à **la frontière applicative**, jamais dans le moteur (ADR-0020, options écartées documentées y compris pourquoi un `.normalize("NFC")` ajouté dans le pipeline de matching aurait cassé le même invariant autrement) : `entity-service.ts` (nom/alias normalisés en NFC à l'écriture, `createEntity`/`updateEntity`) et `tiptap-content.ts` (nouvelle fonction pure `normalizeContentText`, appliquée aux nœuds texte du corps juste avant l'extraction du `plainText` dans `saveEntityContentAction` — body et plainText dérivent ainsi toujours de la même forme normalisée). `aho-corasick.ts` et `normalize.ts` intouchés. Contrôle ponctuel (script jetable, non committé) : aucune donnée existante en base de développement n'était en forme non-NFC — pas de migration nécessaire pour cet environnement, à revérifier sur staging/prod avant bascule. | ✅ corrigé + test de diagnostic committé devenu non-régression (`aho-corasick.test.ts` : limite du moteur pur pinée + preuve que la frontière NFC élimine le cas), tests dédiés `entity-service.test.ts`/`tiptap-content.test.ts`/`entity-content.test.ts`, gates complets — vérification manuelle Aymeric en attente |
