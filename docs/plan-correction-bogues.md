# Plan de correction des bogues — C2.3.2

> Alimenté par les anomalies/régressions détectées en recette.

## SLA / priorisation

- **P0** (bloquant : perte de données, faille de sécurité, prod down) : corrigé sous 24 h.
- **P1** (fonctionnalité majeure dégradée) : corrigé sous 72 h.
- **P2** (mineur, cosmétique) : planifié au jalon suivant.

Processus : détection → qualification (description, impact, reproduction) →
priorisation → diagnostic → correctif **+ test unitaire de non-régression** →
recette → déploiement. Alimenté par les issues GitHub (label `bug`) — pas encore
de repo GitHub actif à ce stade (remote configuré, pas d'issues créées).

| ID bug | Origine (TST-###) | Sévérité | Analyse cause | Correctif | Statut |
|---|---|---|---|---|---|
| BUG-001 | TST-AUT-007 | P1 | Après une erreur de soumission sur `/login` ou `/register` (mauvais mot de passe, e-mail déjà pris, champ manquant), **tous les champs du formulaire se vidaient**, y compris ceux correctement remplis. Cause : React 19 réinitialise les champs non contrôlés d'un `<form action={formAction}>` dès que la fonction d'action **se résout**, même si elle renvoie un état d'erreur — pas seulement en cas de succès (confirmé via issues GitHub React #29034 et #31649). | `src/actions/auth.ts` renvoie désormais les valeurs soumises (`values: { name?, email? }`, **jamais** le mot de passe) à chaque retour d'erreur ; `login-form.tsx`/`register-form.tsx` réappliquent ces valeurs via `defaultValue` sur les champs concernés. Décision produit associée : le mot de passe n'est jamais ré-affiché après une erreur (convention GitHub/GitLab), validée avec Aymeric. | ✅ corrigé + test de non-régression `login-form.test.tsx` (« réaffiche l'e-mail saisi après une erreur, mais jamais le mot de passe ») + validé manuellement par Aymeric |
| BUG-002 | TST-ENT-011, TST-SEC-014 | P1 | Remonté en test manuel le 2026-07-21 : coller un texte externe (Obsidian) dans l'éditeur d'entité faisait échouer **toute la sauvegarde** avec « Contenu invalide. », sans rapport apparent avec la cause réelle. Deux symptômes de la même famille (contenu HTML externe collé, jamais rencontré à la frappe manuelle) : (1) un wikilien converti en `<a href="…">` avec un `href` relatif (parfois avec espaces) échouait `isSafeHttpUrl` (URL `http(s)` absolue exigée) côté validation serveur, qui rejette alors le document entier plutôt que le seul attribut fautif ; (2) un retour à la ligne rendu en `<br>` (au lieu d'un paragraphe distinct) fondait deux lignes du texte source en un seul paragraphe ProseMirror. | (1) Extension `SafeLink` (`src/lib/tiptap-extensions.ts`) : retire la mark `link` dès le parsing (frappe/collage) si `isSafeHttpUrl` (déplacée dans `src/lib/url-safety.ts`, source unique de vérité avec la validation serveur) échoue — le texte de l'ancre est conservé, la validation serveur reste inchangée et non contournable. (2) `splitParagraphsOnBreaks` (`src/lib/tiptap-paste.ts`), câblée via `transformPastedHTML` : scinde un `<p>` contenant des `<br>` en paragraphes distincts au collage (DOMParser, pas de regex), sans paragraphe vide pour des `<br>` consécutifs ou en fin de bloc. | ✅ corrigé + tests de non-régression `tiptap-extensions.test.ts` (6 cas `SafeLink` + cas `splitParagraphsOnBreaks` + intégration collage Obsidian complet), gates complets (lint/tsc/322 tests/build/9 e2e) — vérification manuelle Aymeric en attente |
| BUG-003 | TST-GRF-001 | P2 | Remonté en test manuel le 2026-07-21 (KAN-36 P5) : la disposition des nœuds de la Constellation changeait d'un rechargement à l'autre de la **même** page `/graph`. Cause : `listWorldRelations` (`relation-service.ts`) n'avait aucun `orderBy` — Postgres ne garantit aucun ordre de lignes sans lui — et le layout `cose` de Cytoscape (côté client) traite les arêtes dans l'ordre reçu ; un ordre instable fait converger la simulation physique vers une disposition finale différente à chaque appel, même avec `randomize:false`. (Reste, hors correctif : le panneau du dashboard et la page `/graph` auront toujours des positions différentes l'un de l'autre, tailles de canvas différentes → simulation différente — chantier séparé, persistance d'une disposition canonique, noté au backlog KAN-33.) | `orderBy: [{ createdAt: "asc" }, { id: "asc" }]` ajouté à `listWorldRelations` — `id` (cuid, unique) en second critère lève l'ambiguïté d'un `createdAt` à égalité (lot de relations AUTO créées par le worker dans la même milliseconde). | ✅ corrigé + test de non-régression `relation-service.test.ts` (assertion de l'appel exact `findMany` avec `orderBy`), gates complets (lint/tsc/338 tests/coverage) — vérification manuelle Aymeric en attente |
| BUG-004 | TST-MND-008 | P2 | Remonté en test manuel sur staging le 2026-07-21 : créer une entrée via le bouton « Nouvelle entrée » **du dashboard** ne rafraîchissait jamais la Sidebar une fois revenu dessus (la nouvelle entrée manquait tant qu'aucun rechargement complet n'était fait), alors que la même création depuis le bouton de la **sidebar** semblait fonctionner. **Historique complet des tentatives (aucune réécrite après coup)** : (v1) `revalidatePath("/worlds/[slug]", "layout")` ajouté aux trois actions d'entité — retesté par Aymeric le 2026-07-22, **toujours cassé**. (v2) même correctif corrigé pour inclure le groupe de routes (`revalidatePath("/(app)/worlds/[slug]", "layout")`, la Sidebar étant portée par `(app)/worlds/[slug]/layout.tsx`) — retesté par Aymeric, **toujours cassé**. Les deux tentatives ciblaient le mauvais étage (serveur) ; sur demande explicite d'Aymeric (« STOP aux hypothèses »), diagnostic par preuve : logs temporaires dans `layout.tsx` (compteur serveur) et `entity-search.tsx` (prop reçue vs état affiché à chaque rendu), rejoués via un script Playwright jetable contre le dev server local. **Preuve obtenue** : 10 rendus consécutifs avec `initialEntities.length=1` (prop fraîche, donc le serveur envoyait bien la bonne donnée — les deux tentatives `revalidatePath` n'étaient pas fausses en soi, juste inutiles ici) mais `results.length=0` figé. Cause réelle, `entity-search.tsx:36` : `useState(initialEntities)` ne réutilise l'argument qu'au tout premier montage ; ce composant vit dans un layout persistant à travers toute navigation interne au monde, donc les props plus récentes étaient silencieusement ignorées — un bug **client**, pas serveur. | `entity-search.tsx` : suppression de la copie d'état pour la liste par défaut — sans recherche active, la liste affichée dérive directement de la prop `initialEntities` à **chaque rendu** (`const results = isSearching ? (searchResults ?? []) : initialEntities`). Une recherche active (KAN-17, `searchEntitiesAction`) reste en state (`searchResults`, `null` = pas de recherche) car ses résultats viennent du serveur et ne peuvent pas être dérivés des props. Les deux tentatives `revalidatePath` (v1/v2, inoffensives mais pas la cause) restent en place côté serveur. | ✅ corrigé (v3, cause client) + nouvel e2e de non-régression `e2e/dashboard-create-entity.spec.ts` (création dashboard **et** sidebar, état plié des groupes conservé, recherche active non régressée), gates complets (lint/tsc/341 tests/10 e2e/build) — vérification manuelle Aymeric en attente |
| BUG-006 | TST-SEC-013 | ~~P2~~ **P1 (re-arbitré)** | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1) : upload d'une image valide mais >5 Mo depuis le dialog « Image » de l'éditeur → l'UI reste bloquée sur « Envoi... », aucun message d'erreur affiché à l'utilisateur. Console navigateur : `POST /worlds/eldoria/entities/<id> 500 (Internal Server Error)` puis « An error occurred in the Server Components render. The specific message is omitted in production builds... ». Vérifié : **aucune fuite de sécurité** — aucune ligne `Image` créée en base, aucun objet déposé dans MinIO malgré le `500` (la garde-fou taille bloque bien l'écriture). **Cause confirmée** (logs `docker logs storytide-staging-app-1`, 2026-07-23, en creusant BUG-010) : `Error: Body exceeded 1 MB limit.` (`statusCode: 413`) — la limite **par défaut** des Server Actions Next.js (1 Mo) est atteinte avant même que le contrôle applicatif de taille (5 Mo) ne s'exécute ; jamais relevée via `serverActions.bodySizeLimit` dans `next.config`. **Re-arbitré à la hausse (2026-07-23, plus tard le même jour)** : constat qu'un upload valide de 1 à 5 Mo — dans la capacité annoncée par la spec — échoue aussi (pas seulement le cas >5 Mo testé initialement), ce qui dégrade une fonctionnalité majeure documentée, pas un simple rejet non gracieux d'un cas limite. | Arbitré une première fois (2026-07-23, matin) : P2, report documenté. **Re-arbitré P1, correctif immédiat** (2026-07-23, après-midi) une fois l'impact réel identifié (capacité spec 1-5 Mo cassée). **Corrigé** : `next.config.ts` — `experimental.serverActions.bodySizeLimit: "6mb"` (marge au-dessus de la limite applicative de 5 Mo, qui reste LA limite non contournable, `MAX_IMAGE_BYTES`/`image-service.ts`). Constante et message d'erreur centralisés dans `src/lib/image-validation.ts` (`MAX_IMAGE_BYTES`, `IMAGE_TOO_LARGE_MESSAGE`, `checkImageFileSize` — fonction pure), partagés client (nouveau contrôle immédiat au choix du fichier dans `entity-editor.tsx`, avant tout envoi réseau) et serveur (`image-service.ts`, toujours LA limite qui compte). Tests : `image-validation.test.ts` (2 Mo accepté, 5 Mo pile accepté, 6 Mo et 5 Mo+1 octet rejetés avec le message exact). Gates : lint 0 warning, `tsc` clean, 381/381 tests, couverture 98,13 %. Non vérifiable par test unitaire : le comportement réel de `bodySizeLimit` (config framework, pas de la logique applicative) — à confirmer par upload réel 2 Mo/6 Mo sur staging. | ✅ corrigé, à retester sur staging (nouvelle rc) |
| BUG-007 | TST-SEC-014 | ~~P1~~ **retiré, faux positif** | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1) : coller `<a href="javascript:alert(1)">Cliquez ici</a>` dans l'éditeur (en réalité collé depuis un fichier texte brut, presse-papiers `text/plain` uniquement — confirmé via un écouteur `paste` temporaire en console, `clipboardData.types === ['text/plain']`), sauvegarder, recharger. Inspection DOM (Elements) après rechargement laissait penser qu'une balise `<a href="javascript:alert(1)">` persistait. **Diagnostic corrigé par preuve** (protocole « STOP aux hypothèses ») : `$0.querySelector('a')` → `null` ; `$0.outerHTML` → `<p>&lt;a href="javascript:alert(1)"&gt;Cliquez ici&lt;/a&gt;</p>` (entités échappées) ; `$0.textContent` → la chaîne littérale décodée. **Aucune balise réelle n'a jamais existé** — la coloration syntaxique du panneau Elements de Chrome affiche du texte échappé contenant des chevrons d'une façon visuellement indiscernable d'un vrai élément imbriqué, ce qui a été mal interprété lors de l'inspection initiale. Un collage `text/plain` est intrinsèquement sûr (le navigateur échappe les caractères spéciaux, aucune balise ne peut en résulter) ; le cas `text/html` réel (vrai lien collé depuis une page) reste couvert et passant par `tiptap-extensions.test.ts:101-108` (`generateJSON`, jamais modifié). **Aucune régression, aucun correctif nécessaire.** | Retiré (2026-07-23) après vérification empirique (`querySelector`/`outerHTML`/`textContent` en console) — ne bloque pas le tag v1.2.0. | ⚪ retiré (non reproductible) |
| BUG-008 | TST-MND-008 | P2 | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1), pendant le retest de BUG-004 v3 : la fraîcheur des données de la sidebar est bien corrigée (les trois entrées créées apparaissent sans F5, recherche OK), mais l'**état de repli des groupes** (accordéons par catégorie) se réinitialise à chaque navigation dans le monde (retour à l'accueil puis retour à une fiche), **même sans création d'entrée** — reproduit indépendamment de toute création. Hypothèse non vérifiée par le code (protocole « STOP aux hypothèses ») : possible remontage du composant sidebar/groupes à chaque navigation plutôt qu'une vraie persistance, ce qui contredirait la prémisse du correctif v3 de BUG-004 sans pour autant invalider ce correctif (la liste reste correcte que le composant remonte ou persiste, car dérivée directement des props). Impact : cosmétique/confort uniquement, aucune perte de données ni faille. | Arbitré avec Aymeric (2026-07-23) : P2, report documenté, ne bloque pas le tag v1.2.0 ; recette continue en parallèle. | 🟡 reporté P2 |
| BUG-009 | TST-ENT-009 | P2 | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1) : dans le combobox Type (création/édition d'entrée), `Échap` ferme **tout le dialog** parent au lieu de seulement la liste de suggestions — contredit le critère d'acceptation exact (« `Échap` ferme la liste sans changer la sélection »). Effet observé : les autres champs déjà saisis (nom, alias, etc.) sont perdus, le formulaire est entièrement vidé. Cause non investiguée (protocole « STOP aux hypothèses »). | Arbitré avec Aymeric (2026-07-23) : P2, report documenté, ne bloque pas le tag v1.2.0. | 🟡 reporté P2 |
| BUG-010 | TST-ENT-010 | P1 | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1) : upload d'une image valide (jpg, <5 Mo) échoue systématiquement avec « Envoi impossible pour le moment. » (message générique, `200 OK` côté Server Action). Logs serveur (`docker logs storytide-staging-app-1`) : `[image] Envoi vers le stockage échoué : NoSuchBucket: The specified bucket does not exist`, `BucketName: 'story-tide-staging'` — le bucket MinIO cible n'a jamais été provisionné sur cet environnement staging. Impact : **tout** upload d'image est cassé sur staging, indépendamment de la taille/format (KAN-16 entièrement indisponible). Cause : étape de provisioning du bucket manquante au déploiement staging (à documenter/automatiser pour éviter la même panne en prod). | Arbitré avec Aymeric (2026-07-23) : P1, correctif immédiat (création manuelle du bucket via SSH sur le VPS) pour débloquer la recette (ENT-010/ENT-012), puis à documenter/automatiser dans le processus de déploiement avant le tag v1.2.0. **Automatisé (2026-07-23)** : nouveau service one-shot `minio-setup` (`minio/mc`, `mc mb --ignore-existing` + `mc anonymous set none`, idempotent) ajouté à `deploy/compose.staging.yml` **et** `deploy/compose.prod.yml` (même pattern que `docker-compose.dev.yml`, jusqu'ici jamais répliqué) ; `app`/`worker`/`backup` dépendent désormais de `minio-setup: service_completed_successfully` — plus jamais de démarrage possible sans le bucket. Validé par `docker compose config` (les deux fichiers, variables interpolées correctement). Documenté (`docs/securite-owasp.md` A10, `docs/manuels/deploiement.md`). | ✅ corrigé et automatisé, à vérifier au prochain déploiement réel (rc.2) — **Mise à jour 2026-07-24** : retest effectué sur v1.2.0-rc.2, confirmé réussi (`docs/cahier-recettes.md`, TST-ENT-010) |
| BUG-011 | TST-ENT-010 | P1 | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1), juste après correction de BUG-010 : l'image s'upload correctement (visible dans MinIO), mais ne s'affiche jamais dans l'éditeur — texte alternatif affiché à la place. `/api/media/<imageId>` répond `302`, mais le header `Location` pointe vers `http://minio:9000/story-tide-staging/<key>?X-Amz-...` — **`minio` est le nom d'hôte interne du réseau Docker `storytide-staging_internal`**, inatteignable depuis le navigateur (hors du réseau). La requête suivant la redirection échoue (« Image Request failed », pas de statut HTTP — résolution DNS impossible côté client). Impact : toute image affichée ou rechargée est cassée sur staging, quel que soit l'upload. Cause probable (non confirmée par lecture de code, protocole « STOP aux hypothèses ») : l'adaptateur Storage génère les URLs présignées avec l'endpoint interne (utilisé aussi pour les appels serveur→MinIO) au lieu d'un endpoint public distinct. | Arbitré avec Aymeric (2026-07-23) : P1, correctif requis avant le tag v1.2.0 (investigation de config/code nécessaire, pas une action ponctuelle VPS) ; ENT-010/ENT-012 restent en attente de ce correctif, recette continue sur les autres scénarios (GRF, QOT) en parallèle. **Correctif appliqué (2026-07-23)** : exposer MinIO publiquement via Traefik aurait cassé `TST-SEC-011` (MinIO jamais joignable depuis Internet) — retenu à la place : `GET /api/media/[imageId]` (`route.ts`) proxy désormais le binaire (`fetch` serveur→MinIO via l'URL signée, résolue en interne, puis `Response` streamée au client) au lieu de rediriger (`Response.redirect`) le navigateur vers l'URL signée elle-même. Aucun changement d'infra/env/port `Storage`. Ce fetch n'est pas un SSRF (hôte = config serveur, jamais une entrée utilisateur — documenté `docs/securite-owasp.md` A10). Test de non-régression : `route.test.ts` (proxy du contenu avec le bon `Content-Type`, jamais de header `location`, `502` si le stockage est injoignable ou répond en erreur — erreur réelle toujours logguée, jamais avalée). Gates : lint 0 warning, `tsc` clean, 377/377 tests unitaires. | ✅ corrigé, à retester sur staging (ENT-010/012) — **Mise à jour 2026-07-24** : retest effectué sur v1.2.0-rc.2, confirmé réussi (`docs/cahier-recettes.md`, TST-ENT-010/TST-ENT-012) |
| BUG-012 | TST-GRF-004 | P2 | Remonté en recette staging le 2026-07-23 (v1.2.0-rc.1) : le type d'un nœud de la Constellation n'est identifiable que par sa couleur (famille) — aucun tooltip au survol/clic, seul le nom de l'entité est affiché sous le nœud, le type n'apparaît nulle part au niveau du nœud individuel (le libellé textuel des chips concerne le filtre, pas l'identification d'un nœud déjà visible). Contredit le critère d'acceptation exact de GRF-004 (« couleur jamais le seul moyen d'identifier le type précis ») — manquement RGAA/WCAG 1.4.1 (usage de la couleur), dans le périmètre C2.2.3 (accessibilité, éliminatoire). Le clic navigue vers la fiche où le type est affiché (badge), limitant l'impact pratique. | Arbitré avec Aymeric (2026-07-23) : P2, report documenté, ne bloque pas le tag v1.2.0. | 🟡 reporté P2 |
| BUG-013 | (trouvé ad hoc, validation locale de BUG-006) | P2 | Repéré le 2026-07-23 en validant BUG-006 localement : dans le dialog « Image », un nom de fichier long (ex. `0CdUaD9wS2frjQot0e9I3t6IrAnBsKXTez5UEamYp99lddanORlZP2i9VE9X.jpg`) élargissait et décalait tout le dialog au lieu d'être tronqué. Cause : `DialogContent` (`dialog.tsx`) est en `grid` — l'enfant direct de la grille (`<div className="flex flex-col gap-1.5">` englobant le nom de fichier) ne rétrécit pas sous la largeur intrinsèque de son contenu sans `min-w-0` explicite (même piège que `min-width: auto` en flex, propre à grid). Un premier correctif posant `truncate`/`min-w-0` seulement sur le `<p>` (un niveau trop profond, pas l'enfant direct de la grille) n'a pas suffi. | `min-w-0` ajouté sur le conteneur (l'enfant direct de la grille), en plus de `truncate`/`min-w-0`/`title` sur le `<p>` lui-même (nom complet conservé au survol). Validé en local par Aymeric (plus de décalage, troncature correcte). | ✅ corrigé, validé en local |
| BUG-005 | TST-LNK-008 | P1 | Diagnostiqué proactivement le 2026-07-22 (pas remonté en recette) : hypothèse initiale d'Aymeric à vérifier avant tout correctif, protocole de preuve empirique par test Vitest imposé (« STOP aux hypothèses »). Un texte en forme Unicode **décomposée** (NFD — accent = caractère combinant séparé, ex. copier-coller depuis macOS/export tiers) matche correctement en isolation, mais `AhoCorasick.search()` (`aho-corasick.ts`) calcule ses positions sur la chaîne normalisée par `normalizeForMatch` (`normalize.ts`, transformation sur la chaîne ENTIÈRE, pas caractère par caractère) puis les réutilise telles quelles contre le texte **original** pour la vérification de frontière de mot — un texte NFD plus tôt dans le document décale cette vérification et fait **disparaître silencieusement** une mention ultérieure, **même non accentuée**, donc jamais de `Relation origin=AUTO` ni de surlignage pour elle. Preuve empirique par test Vitest (`aho-corasick.test.ts`) avant tout correctif, conformément au protocole demandé. | Normalisation NFC à **la frontière applicative**, jamais dans le moteur (ADR-0020, options écartées documentées y compris pourquoi un `.normalize("NFC")` ajouté dans le pipeline de matching aurait cassé le même invariant autrement) : `entity-service.ts` (nom/alias normalisés en NFC à l'écriture, `createEntity`/`updateEntity`) et `tiptap-content.ts` (nouvelle fonction pure `normalizeContentText`, appliquée aux nœuds texte du corps juste avant l'extraction du `plainText` dans `saveEntityContentAction` — body et plainText dérivent ainsi toujours de la même forme normalisée). `aho-corasick.ts` et `normalize.ts` intouchés. Contrôle ponctuel (script jetable, non committé) : aucune donnée existante en base de développement n'était en forme non-NFC — pas de migration nécessaire pour cet environnement, à revérifier sur staging/prod avant bascule. | ✅ corrigé + test de diagnostic committé devenu non-régression (`aho-corasick.test.ts` : limite du moteur pur pinée + preuve que la frontière NFC élimine le cas), tests dédiés `entity-service.test.ts`/`tiptap-content.test.ts`/`entity-content.test.ts`, gates complets — vérification manuelle Aymeric en attente |
